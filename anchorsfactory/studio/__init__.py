"""AnchorsFactory Studio — a local, browser-based visual debugger for anchor
rules.

Optional, dependency-free (stdlib ``http.server`` + the engine's own
``fontParts``/``fontTools``). Launched via the ``anchorsfactory-studio`` console
script or ``python -m anchorsfactory.studio``. Imports the engine's public API
only — the engine never depends on this package.
"""
